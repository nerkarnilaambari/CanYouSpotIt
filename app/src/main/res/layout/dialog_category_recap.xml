<?xml version="1.0" encoding="utf-8"?>
<!-- All-time category recap, shown as a styled Dialog when leaving the Learn screen.
     Card fill is a theme alias (recap_dialog_card): light = accent_dusty_pink to tie
     into Learn's theme; dark = cream, because on the dark palette the dusty-pink card
     left too little contrast with the olive "Got it" button. Work Sans typography,
     standard accent_cta button. Deliberately NOT a default system AlertDialog. -->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="28dp">

    <androidx.cardview.widget.CardView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:cardCornerRadius="24dp"
        app:cardElevation="2dp"
        app:cardBackgroundColor="@color/recap_dialog_card">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="28dp">

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="You're getting good at spotting:"
                android:fontFamily="@font/work_sans_bold"
                android:textSize="20sp"
                android:textColor="@color/forest_shade"
                android:layout_marginBottom="16dp" />

            <TextView
                android:id="@+id/tvRecapCategories"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text=""
                android:textAppearance="@style/TextAppearance.CanYouSpotIt.Body"
                android:textColor="@color/forest_shade"
                android:textSize="16sp"
                android:lineSpacingExtra="8dp"
                android:layout_marginBottom="24dp" />

            <Button
                android:id="@+id/btnRecapGotIt"
                android:layout_width="match_parent"
                android:layout_height="52dp"
                android:text="Got it"
                android:textAppearance="@style/TextAppearance.CanYouSpotIt.ButtonLabel"
                android:textAllCaps="false"
                android:textColor="@color/on_accent_cta"
                android:backgroundTint="@color/accent_cta"
                android:insetTop="0dp"
                android:insetBottom="0dp"
                style="@style/Widget.MaterialComponents.Button"
                app:cornerRadius="26dp" />

        </LinearLayout>

    </androidx.cardview.widget.CardView>

</FrameLayout>
